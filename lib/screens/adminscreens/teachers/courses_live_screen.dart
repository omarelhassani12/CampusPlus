import 'package:flutter/material.dart';

import '../../../utils/colors.dart';

class LiveVideoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Live Video Screen'),
      ),
    );
  }
}
